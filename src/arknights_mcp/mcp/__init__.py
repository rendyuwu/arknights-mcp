"""MCP tool registry, envelopes, resources (transport-agnostic).

The typed response envelope (:mod:`arknights_mcp.mcp.envelopes`) and the single
shared tool registry (:mod:`arknights_mcp.mcp.tool_registry`) live here; both
transports build on the same registry + envelope shape (§V14).
"""
