# SPEC

src: Arknights_MCP_PRD_v0.1_FINAL.md + Arknights_MCP_Agent_Execution_Prompt_FINAL.md. founder decisions §18 D1-D15 binding; change → ADR.

## §G GOAL

read-only Python 3.12 Arknights Intelligence MCP. 1 shared core → 2 transports (local `stdio` + private OAuth/OIDC Streamable HTTP). import allowlisted EN/CN snapshots → versioned SQLite. expose stage/enemy + operator/module intel w/ region + provenance + deterministic evidence-backed analysis. ⊥ query-time upstream fetch.

## §C CONSTRAINTS

- Python 3.12 + `uv` (env, lockfile, run).
- MCP Python SDK v1: `mcp>=1.28.1,<2`, exact resolved version in `uv.lock`. SDK v2 pre-release ∴ ⊥ until ADR.
- Pydantic v2. SQLAlchemy Core (⊥ heavy ORM) + explicit migrations (Alembic | small runner). SQLite 3.51.3+. ASGI server for Streamable HTTP.
- pytest + pytest-cov + Hypothesis. Ruff. mypy | pyright.
- Apache-2.0 project code only. `NOTICE` excludes imported data + game content.
- 1 shared core, 2 transports. ⊥ duplicate domain logic across modes.
- separated layers: source adapters, field policy, importers, repositories, analyzers, MCP schemas, transports, auth, middleware.
- data acquire: user local snapshots + explicit allowlisted repo sync only. ⊥ arbitrary URL downloader.
- code-only distribution. ⊥ bundled raw snapshot | prebuilt DB.
- private + noncommercial v0.1. public service = separate readiness gate (out of scope). ⊥ single public-mode flag.
- ⊥ wiki/community prose, art, audio, story, voice, full announcement bodies. announcements metadata-only (v0.1 disabled → v0.2 ENABLED by default after M9 policy review, T93/D14/§V56); full announcement BODY ⊥ ever.
- ⊥ game login | roster storage | squad optimizer | combat sim | banner/gacha PLANNING (pull-probability | spark | rate-pity | shop forecast). v0.2 adds banner ARCHIVE (historical FACT: pool id/name/open/end/rule-type + TYPED featured-op; §V62) from primary `arknights_assets_gamedata` (⊥ new source) — archive ≠ planning (D5 deferred → carve, ADR-gated, roadmap §6.3); PLANNING still ⊥ (rate/pity/spark constants VERIFIED absent from `gacha_table` ∴ would need hardcoded non-snapshot constants, deferred).
- farming-strategy PLANNER (item-target → per-stage run-count LP/ILP solve, à la penguin planner/calculator) = DEFERRED, ADR-gated: needs (a) `building_data.json` synthesis-recipe import — NOT in allowlist nor §V41 `SUPPLEMENTARY_FILES` ∴ new source file + migration + importer; (b) LP/ILP solver dep (⊥ current deps); (c) reconcile w/ §V7/§V55 (a "plan" = optimal recommendation ⊥ mandatory/best-farm) + §C optimizer-exclusion class. v0.2 ships item→stage drop COMPARISON (§V60) only, ⊥ the solver.
- regions: FACT regions `en`,`cn` (v0.1+v0.2). v0.2 adds extra-locale NAME aliases (jp/kr, §V57) — alias locale ≠ fact region, ⊥ new fact regions.
- sources: primary `arknights_assets_gamedata`; CN validator `kengxxiao_gamedata` (CI only). v0.2 ENABLES `penguin_statistics` (drop-rate cache, CC BY-NC 4.0, NC-only, §V52-V55) + official announcement adapter (metadata-only, D14/§V56).
- CI: lint + type + test on Linux/`ubuntu-latest` only. ⊥ Windows|macOS (Docker-based ∴ OS-portable; no macOS device ∴ ⊥ real test).

## §I INTERFACES

CLI (`arknights-mcp`, admin-only, may touch allowlisted net):
- cmd: `arknights-mcp sync --server en|cn|all` → build candidate DB from allowlisted source
- cmd: `arknights-mcp import --server en --source-path ./snapshot/en` → build candidate from local snapshot
- cmd: `arknights-mcp validate --database <path>` → integrity/FK/golden report
- cmd: `arknights-mcp status` → active snapshot + schema version
- cmd: `arknights-mcp doctor` → health (versions, DB, sources, transport, config warnings); ⊥ print secrets
- cmd: `arknights-mcp source list | enable <id> | disable <id> | purge <id> --rebuild`
- cmd: `arknights-mcp serve --transport stdio|streamable-http`

MCP tools (read-only, same registry both transports, typed envelope: `schema_version`,`status`,facts/data,`provenance`,`limitations`,`analyzer_version`):
- tool: `search_entities` `get_operator` `compare_operator_modules` `get_enemy` `search_stages` `get_stage` `analyze_stage` `get_data_status` `get_data_sources` `get_stage_drops` `get_item_drops` `get_announcements` `get_banners` (v0.2). `search_entities` gains additive `locale` param (v0.2, §V57)

MCP resources (optional, read-only):
- resource: `arknights://operator/{server}/{game_id}` `arknights://enemy/{server}/{game_id}` `arknights://stage/{server}/{stage_id}` `arknights://status/{server}` `arknights://sources` `arknights://announcements/{server}` `arknights://banners/{server}` (v0.2)

Files:
- file: `config.toml` (`config.example.toml`) → `[database][sync][analysis][mcp][auth][limits][privacy]`
- file: `config/data_sources.toml` → machine source registry
- file: `data/current.json` → atomic manifest (immutable filename, hash, schema version, snapshots)
- file: `data/builds/<ts>-en-cn.sqlite` → versioned immutable build
- file: policy docs `DATA_SOURCES.md` `DATA_POLICY.md` `TAKEDOWN_POLICY.md` `PRIVACY.md` `NOTICE` `SECURITY.md`

Env + transport:
- env: OAuth/OIDC secrets via env only (⊥ TOML): issuer, audience, jwks_url, required_scopes
- api: Streamable HTTP `POST /mcp` over HTTPS, bind `127.0.0.1:8000` behind reverse proxy; unauthenticated `GET /.well-known/oauth-protected-resource`(+`/mcp`) = RFC 9728 discovery (§V45)
- stdio: MCP protocol → stdout; logs → stderr

## §V INVARIANTS

V1: runtime MCP tool ⊥ outbound source-network request. only CLI sync/import → allowlisted sources.
V2: SQLite opened read-only ∀ MCP process. parameterized SQL only. ⊥ arbitrary SQL | shell | fs | source-download tool.
V3: failed | schema-incompatible sync ⊥ replace current DB (fail closed).
V4: candidate promote only after validate pass: `PRAGMA integrity_check` + `PRAGMA foreign_key_check` + critical-table + row-count + golden + FTS5 `integrity-check`. `PRAGMA integrity_check` ⊥ verify FTS5 shadow tables ∴ run FTS5's own `INSERT INTO entity_fts(entity_fts) VALUES('integrity-check')` (prepared as write → short-lived `mode=rw` handle; no writes → candidate byte-identical). promotion atomic via `current.json`. ⊥ mutate active DB in place.
V5: ∀ factual response → region ∈ {en,cn} + provenance (`snapshot_id`,`imported_at`). en & cn ⊥ silently mixed.
V6: ∀ observation → `rule_id` + evidence + confidence + exceptions/limitations + `analyzer_version`.
V7: recommendations capability-based & conservative. ⊥ "mandatory" | universal-best label (v0.1: never).
V8: confidence `< 0.5` → ⊥ recommendation; report as limitation.
V9: non-loopback remote w/o HTTPS + valid OAuth/OIDC settings → fail startup. authless non-loopback ⊥ (except loopback dev).
V10: remote bearer validated: issuer + audience + expiry + JWKS signature + required scope. ⊥ password/username storage. resource-server contract (VERIFIED vs live Auth0 M2M token): decode alg RS256 only (`algorithms=["RS256"]`; ⊥ `none`|HS* ∴ ⊥ symmetric-key confusion); select JWKS key by header `kid` (PyJWKClient cache; unknown `kid` → refetch JWKS ∴ key rotation); validate signature + `iss` (exact, incl trailing slash) + `aud` + `exp` + `iat` (require exp/iat/iss/aud; `leeway=60s`); `aud` = str | array (Auth0 emits array once `openid` added) → accept both; required-scope = AND (∀ required ∈ granted); granted = `scope` (space-delim str) ∪ `permissions` (array) (Auth0 client-credentials emits `scope` only after API perm granted to M2M app); absent/insufficient scope → typed `insufficient_scope` reject; principal id = `iss|sub` (subject unique only per issuer); client_id = `azp`; verify fail → return None past auth backend (typed reject; ⊥ token|secret in msg|log).
V11: remote enforces per-principal rate limit + concurrency limit + request timeout + request cap + response cap.
V12: default logs ⊥ full prompt | full tool args | response body | authorization header | bearer token | raw source record | roster/account.
V13: `stdio` → MCP protocol on stdout only; logs on stderr only.
V14: both transports → same `tool_registry` + services. same DB + same input → identical domain result. ⊥ duplicate domain logic.
V15: ⊥ request | store | transmit Arknights game credentials.
V16: release artifact ⊥ raw snapshot | prebuilt DB | artwork | audio | story script | voice line | wiki/community prose | full announcement body.
V17: ∀ imported record → `snapshot_id` + `source_path`/key + `transform_version` + `record_hash`.
V18: importer applies explicit field allowlist; excludes unused prose. imported string = untrusted data; ⊥ concat into server instructions | tool descriptions; strip control chars; cap length.
V19: ⊥ bulk dump | DB download | unbounded pagination | enumeration → dataset reconstruction. search limit default 10 max 50; page_size max 100. out-of-range limit *rejected* at BOTH the model (`SearchEntitiesInput` `ge/le`) AND the service (`search_entities` → `ValueError`); ⊥ silent clamp|widen in either (one contract, same enforcement both places).
V20: `disable` → stop new sync, keep current data. `purge --rebuild` → remove only rows attributable to source; current DB active until rebuilt candidate validates.
V21: required tool fields backward-compat within v0.1; additive optional fields preferred; breaking change → `schema_version` bump + ADR.
V22: default tool response `< 200 KB`. large map/spawn → explicit include flag | pagination. cap measured at worst-case ASCII-escaped byte length (`ensure_ascii=True`, ≥ raw UTF-8 ∀ char) ∴ cap holds on wire regardless of transport encoding (compact UTF-8 | `\uXXXX` escapes); fail-closed = over-measure, ⊥ under-measure.
V23: ∀ tool result → typed status ∈ {ok,partial,not_found,ambiguous,unsupported_server,data_stale,database_unavailable,schema_incompatible,analysis_unavailable,internal_error}. error ⊥ stack trace | local path.
V24: absent entity → typed not_found | region_unavailable | data_stale + suggested admin action. ⊥ query-time download/scrape fallback.
V25: `mcp>=1.28.1,<2`; exact resolved version in `uv.lock`. SDK v2 migration → ADR.
V26: analyzer rules on typed fields (⊥ NL keyword match). missing field → reduce confidence | limitation. conflicting source fields → omit conclusion + warn.
V27: source registry complete ∀ enabled source: `source_id` + owner + URL + purpose/domains + regions + license/permission status + attribution + enabled + `last_reviewed` + snapshot commit. `get_data_sources` ⊥ secrets | local fs path | OAuth config | takedown correspondence.
V28: admin ops (sync, import, validate, purge, source mgmt) CLI-only. ⊥ exposed as MCP tool.
V29: importer parse contract verified vs real `arknights_assets_gamedata` schema; ⊥ validate data path solely on synthetic fixture matching parser. real shapes: `enemy_database.json` top-level id-keyed dict → list `{level, enemyData.attributes.<stat>.m_value}` (no `"enemies"` wrapper; `maxHp`/`magicResistance`/`baseAttackTime` ≠ `hp`/`res`/`attackInterval`); `stage_table.levelId` = `Obt/Main/level_main_04-04` (Title-case, no `gamedata/levels/` prefix, no `.json`); level tiles grid-indexed (no `x`/`y`; `passableMask` ≠ `passable`), wave action enemy under `key` ≠ `enemyId`; enemy motion @ `enemy_database` `enemyData.motion.m_value` (handbook no `motionType`). field maps VERIFIED vs LIVE upstream @`413a81a3ff3e` (T68, ⊥ "inferred"): `maxHp`→hp, `magicResistance`→res, `baseAttackTime`→attackInterval, `massLevel`→weight, `lifePointReduce`→lifePointReduction, `motion`→motion_type, `preDelay`→spawnTime, `maxTimeWaitingForNextWave`→maxTimeWaiting, positional route/wave index; real 4-4 → non-null hp/res/attackInterval/weight/lifePointReduction/motion + non-empty tiles/spawns/`stage_enemies`.
V30: raw upstream shape ≠ normalized tables → explicit `importers/normalization.py` transform ! bridge. sync/import ! report per-stage import counts; non-empty source yielding 0 levels/tiles/spawns/`stage_enemies` → fail closed (⊥ silent empty build). the silent-empty guard is PER SOURCE-DOMAIN, ⊥ aggregate: a source feeding ≥2 target domains (extra-locale → operator + enemy, §V57) landing 0 in ONE domain while a sibling succeeds → STILL fail closed for the missed domain (⊥ a sibling's success mask a per-domain game_id-scheme mismatch) (B51).
V31: field allowlist + sanitize applied recursively ∀ nested string leaf in kept JSON (dict|list) value + stored source fragment; ⊥ store raw unallowlisted dict|list; cap serialized size before JSON-encode.
V32: `purge --rebuild` cascade-deletes ∀ table tracing to purged source (children→parents, FK on): stage/enemy/zone + operator/skill/module/talent domains. residual FK from a non-purged source → `PurgeError` (fail-closed; ⊥ uncaught `IntegrityError`; ⊥ strip a non-purged stage's occurrences). purge ! flip machine-registry `enabled=false` (takedown = disable+purge). purge policy-event journaled durably only after candidate validates+promotes (⊥ phantom event on failed rebuild). purge ! rebuild `entity_fts` (standalone FTS5, no triggers) from surviving rows ∴ ⊥ stale search doc for a purged source (else taken-down entity keeps surfacing via name/alias/game_id, V16).
V33: importer maps source constraint anomaly (dup PK | variant | index) → typed `ImporterError`; ⊥ uncaught sqlite `IntegrityError` tearing down multi-region build.
V34: 1 public-safe source projection fn shared by `source list` + `get_data_sources`; ⊥ divergent field allowlists across the 2 surfaces. both surfaces ! route the SAME projection (`registry.public_view`); ⊥ 2nd independent field enumeration (e.g. service `SourceInfo`) re-forking the allowlist. regression test ! assert set-equality of emitted keys minus DB-only enrichment (`active_snapshots`); `policy_notes`-disjoint + N named-field checks = weak, misses re-fork (B18).
V35: analyzer summary count = distinct `evidence.ref`, ⊥ raw occurrence rows → 1 entity across ≥2 level variants counts once.
V36: stage `levelId` → level-file access confined to levels tree: normalized path ! startswith `gamedata/levels/` + endswith `.json`; ⊥ `.`|`..` segment; ⊥ nested `gamedata`|`excel` segment. `normalize_level_id` ! force `gamedata/levels/` prefix ∴ crafted `levelId` ⊥ fetch excel table | escape tree (L8 post-T66-normalize). confinement guard = 1 shared fn (`normalization.is_clean_level_path`, §V37) applied at BOTH the network discovery path (`arknights_assets._discover_level_paths`) AND the local import read path (`stages.import_stages`, before `adapter.exists`/`read_json`) — adapter root-confinement (`_safe_path`) alone ⊥ sufficient: a `levelId` folding back into `gamedata/excel/` via `..` stays inside the snapshot root yet must still be refused (B32).
V37: DRY. shared logic exactly 1 home. ⊥ copy-paste fn|SQL|guard|const across modules. N≥2 identical|near-identical copies → extract to shared util|importer helper. behavioral variant ! explicit param|arg — ⊥ silent divergent copies (e.g. `_as_str` sanitize in 1 module ≠ raw in another). extends V14 (cross-transport dedup) → intra-codebase.
V38: py source module ≤ 500 lines target, ⊥ > 800 hard cap (excl. generated lockfile). module accreting ≥3 distinct responsibility groups (e.g. CLI command groups `sync`|`import`|`validate`|`status`/`doctor`|`source`) → split to package: 1 module/group + shared `_shared` helpers. ⊥ 1 file mixing parse+DB+HTTP concerns (per §C separated layers).
V39: spawn-time analytics only on a comparable time base. importer `spawn_time` = fragment-relative `preDelay` (upstream `spawnTime` null → `normalization.py:372` fallback), ⊥ stage-absolute; wave+fragment `preDelay` offsets dropped (`_normalize_waves` keeps fragment `actions` only) ∴ `stage_enemies.first/last_spawn_time` = min/max preDelay across ALL waves ≠ elapsed window. rule ⊥ conclude burst|window from cross-wave first/last min/max (extends §V26 typed-field-only). ∴ pick: (a) persist wave+fragment offset → absolute base pre-aggregate, | (b) confine window to 1 wave+fragment, | (c) downgrade confidence + record limitation "spawn window fragment-relative, aggregated across waves; may overstate burst".
V40: remote auth enforcement INDEPENDENT of bind address. loopback bind skips §V9 HTTPS+OAuth startup gate + OIDC enforcement ONLY when auth unconfigured (genuine local dev). reverse-proxy|tunnel binds loopback yet serves public internet ∴ config ! carry explicit intent `[mcp.remote] behind_proxy=true` → forces §V9 gate (HTTPS assumption + valid OIDC) + bearer enforcement even on `127.0.0.1` bind. ⊥ equate loopback w/ authless-trusted when `behind_proxy`. fail closed.
V41: network `sync` staged-file set (`arknights_assets.CORE_FILES` ∪ `SUPPLEMENTARY_FILES`) ! superset of ∀ `gamedata/*.json` source path any pipeline importer (`import_enemies`/`import_stages`/`import_operators`/`import_modules`) reads by DEFAULT ∴ every in-scope domain (PRD §6.1: enemy+stage+operator+module) is actually fetched by a real sync. operator/module tables are optional-zero at the pipeline (`operators.py:594`/`modules.py:386` return empty if the table absent) ∴ their omission from the fetch set is NOT caught by §V30 (combat-scoped) → a silent `operators=modules=0` build w/ empty `get_operator`/`compare_operator_modules` (B36). ∴ `SUPPLEMENTARY_FILES` (character_table, skill_table, uniequip_table, battle_equip_table; `uniequip_data`=`uniequip_table.equipDict`, ⊥ separate file) fetched every sync via shared `_download_optional` (§V37, tolerant 404/410 skip+warn per B34 — combat-only snapshot legitimately lacks them ∴ ⊥ mandatory core, but ! attempted). introspection test asserts importer-default `gamedata/**.json` paths ⊆ staged set ∴ adding an importer domain w/o wiring its source file → test fails.
V42: `sync` download concurrency (keep-alive connection reuse + bounded parallel pool, T79) ⊥ weaken ANY per-file §V1 gate: HTTPS-only, per-file byte cap, JSON depth/node cap, allowlist `_validate_relative_path`, same-domain redirect policy — ∀ still applied per file regardless of worker. shared run-level `DownloadBudget.charge` ! thread-safe (Lock; `self._used += n` ⊥ atomic under threads) ∴ total-download cap (PRD §11.2) stays EXACT + still fail-closed under N workers (⊥ overshoot via lost update; ⊥ TOCTOU past cap). staged output deterministic ⊥ dependent on completion order (each file written to its normalized path, ⊥ shared mutable order). B34 404-skip + `_download_optional` missing-count aggregation preserved under parallelism (count = Σ across workers, exact). `[sync].max_parallel_downloads` bounds concurrency (default 8; =1 → serial fallback, ⊥ removed). ⊥ unbounded fan-out (2257 refs ⊥ 2257 sockets). ordering dep intact: `stage_table` fetched+parsed BEFORE level discovery fans out.
V43: wave-action spawn→enemy resolution keys PER-REF on the `enemyDbRefs` ref's `useDb` flag, ⊥ on id-membership. `useDb:true` → ref `id` is a real `enemy_database`/handbook enemy, spawn resolves direct. `useDb:false` = level-inline variant (`enemy_..._a`/`_b`/`_2a`…; stats live inline in `overwrittenData`, NEVER in the enemy tables) → resolve spawn FK to `overwrittenData.prefabKey.m_value` (base enemy, always ∈ DB upstream) + carry the inline id as `variantId` in the allowlisted `source_fragment`. per-ref ∵ same id may be db-backed in one stage + inline in another (10 EN ids), same inline id → different prefab base across levels (9 EN ids) ∴ ⊥ global inline→enemy map. genuinely-unresolvable spawn (inline w/o `prefabKey` \| `useDb:true` id absent from DB) STILL fails closed at `levels.py` cross-ref check (§V3/V4 preserved; ⊥ fabricate an enemy row). RESOLVED T80 (§V46): inline `overwrittenData` per-variant stats now persisted as stage-scoped variant rows; threat analytics read variant stats OVER base (COALESCE). RESIDUAL: only §V29-verified scope (attributes+motion+lifePointReduce) overridden — tags/immunes NOT §V29-verified ∴ ⊥ overridden, inline-variant tag/immune spawn still attributes to base prefab.
V44: `enemy_database` level entries = DELTAS over level 0. importer ! gate each mapped attr on its `m_defined` flag (`enemyData.attributes.<stat>.m_defined` + `enemyData.<scalar>.m_defined`): explicit `m_defined:false` = attr UNSET this level (sentinel `m_value`, typically 0, ⊥ a real stat) → inherit base (`level==0`) value; `m_defined:true` \| flag absent (shorthand/already-normalized) = defined → keep `m_value` incl genuine 0. `_normalize_enemy_level` drops `m_defined:false` cells (`_defined_m_value`); `_apply_level_deltas` backfills each higher level's missing mapped keys (`_INHERITED_LEVEL_KEYS` = ∪ `_ENEMY_STAT_MAP` + `_ENEMY_DATA_SCALAR_MAP`) from the level-0 entry. ⊥ write sentinel 0 as res/attackInterval/moveSpeed/weight/lifePointReduction (B38: a spawned level>0 variant → `db/repositories/stages.py:137` threat LEFT JOIN keys on `se.enemy_level_variant` → reads THAT variant row → false-0 def/res fed to def_res_skew/ranged_arts/block_bypass + `get_enemy`). extends §V29 (field-NAME maps) w/ per-level define semantics; same one-flag-not-read class as §V43 (`useDb`) / B35 (`actionType`).
V45: interactive OAuth bootstrap. remote serves RFC 9728 Protected Resource Metadata UNAUTHENTICATED @ `/.well-known/oauth-protected-resource` (+ resource-path suffix, e.g. `.../oauth-protected-resource/mcp`) so an MCP OAuth client (`claude mcp login`) discovers the AS from a 401 (⊥ hand-pasted bearer). doc = `{resource: public_base_url+path (= token aud), authorization_servers: [issuer], scopes_supported: required_scopes, bearer_methods_supported: ["header"]}`; ⊥ secret (issuer/url/scope only). discovery layer OUTSIDE bearer, INSIDE logging (`_ProtectedResourceMetadataASGIApp` above `_BearerAuthASGIApp`) ∴ GET/HEAD well-known path answered 200 pre-auth while `/mcp` STILL bearer-gated 401 (§V10 preserved — only the 2 well-known paths bypass). advertise issuer ONLY; ⊥ serve|proxy AS metadata (`/.well-known/oauth-authorization-server`) — client fetches it straight from Auth0 ∴ ⊥ query-time network (§V1). bearer 401/403 challenge carries `resource_metadata="<prm url>"` (RFC 9728 §5.1) — config, ⊥ token|secret (§V12). metadata layer only on auth-requiring path (§V40; wired in `wrap_remote_app`), absent in authless dev.
V46: `useDb:false` inline wave-action variant (§V43) → STAGE-SCOPED `stage_enemy_variants` row, ⊥ global enemy row (same inline id → DIFF `prefab_base` across levels [9 EN ids], + id db-backed elsewhere [10 EN ids] ∴ global row impossible) w/ `prefab_base` FK + provenance + region. migration 0008: `stage_enemy_variants`; `stage_spawns.variant_pk`; `stage_enemies` recreated w/ `variant_pk` ∈ PK ∴ two variants of one base+level ⊥ collapse. `normalization.py` reuses §V29-verified `attributes.<stat>.m_value`/motion maps + §V44 `m_defined` gating → extract ONLY defined stat overrides from `overwrittenData`; prose (name/description) stripped via `OVERWRITTEN_DATA_ALLOWLIST` (§V18). `get_stage` + threat analyzers read variant def/res/motion OVER base via COALESCE; `variant_id` surfaced additive (§V21). unresolvable prefab STILL fails closed (§V3/V4/§V43 preserved); purge cascades variants (§V32); `variant.prefab_base` region-checked (§V5). SCOPE = §V29-verified stats only (attributes+motion+lifePointReduce); tags/immunes NOT §V29-verified ∴ ⊥ overridden (residual §V43). `_collect_variants` (variant ROWS) + `_enemy_ref_map` (spawn `variantId`) resolve the SAME refs ∴ MUST mirror EXACTLY: same `enemyDbRefs`→`enemies` source fallback + same per-id last-wins dedup — else a spawn's `variantId` dangles w/o a variant row (`variant_pk` NULL → overrides silently dropped back to §V43, NO error) \| a duplicate inline id → 2 rows same id → `UNIQUE(stage_pk, variant_id)` collision → build aborts (B39). same two-paths-must-agree class as B26.

V47: `analyze_stage(depth="detailed")` ! emit the per-enemy stat block its tool description promises. detailed occurrence = standard identity/timing PLUS the typed stat block (hp,atk,def,res,attack_interval,move_speed,weight); ⊥ advertise "full per-enemy stat/timing context" (`mcp/tools/stage.py:231`) while `_occurrence_full` (`:247`) + `EnemyOccurrenceFacts` omit the stats (B41). one contract: EITHER shaper carries the promised fields OR description reworded to the emitted subset — ⊥ desc ⊃ output. detailed reads variant stats OVER base (§V46 COALESCE). integration test asserts the detailed contract.
V48: MCP tool/model description strings = part of the client contract (§V21) ∴ ! name the module-comparison axis "module levels" (1/2/3), ⊥ "potential levels". module upgrade tier ≠ operator potential (potential-dependent talent variants = a SEPARATE nested axis, §T45); a client reads the text literally ∴ mislabel → wrong tool selection (B40). doc-terminology regression test pins the wording.
V49: analyzer lane/route rule ⊥ equate raw stage `route_count` (count of raw route RECORDS) with player-facing lane count. records share start/end/checkpoint geometry ∴ raw count overstates lanes (4-4: 26 records ≠ 26 lanes). pick: (a) cluster records → effective-lane groups + headline the group count, | (b) label evidence "raw route records" + record limitation "raw route count ≠ distinct lanes; geometry not clustered" + downgrade confidence. same raw-field-≠-semantic-claim class as §V39 (preDelay) / §V44 (m_defined). (B43)
V50: `search_entities`/`search_stages` ! honor §V24 region availability BEFORE asserting absence. region ∉ {en,cn} → `unsupported_server`; region ∈ {en,cn} w/ NO active snapshot → `data_stale` + suggested admin action; ⊥ bare `not_found` (that asserts the entity absent — ⊥ inferable when the region index is empty). `not_found` only after the region index is confirmed present. extends B24 (region verdict wired into `arknights://status` resource) to the search tool surface — same one-surface-not-both class. (B42)
V51: stage route `checkpoints` wire shape = ALWAYS a JSON array; empty → `[]`, ⊥ `{}` | raw source dict passthrough. `get_stage` RouteFacts shaping normalizes the decoded fragment to a list ∴ a client indexes `checkpoints` as an array unconditionally (schema-consistency, §V21 stable contract). (B44)

V52: v0.2 `penguin_statistics` drop-rate cache ⊥ weaken §V1: penguin fetched ONLY @ CLI sync/import, ⊥ any query-time MCP call. drop cache served from read-only SQLite only (§V2). same import-time-only class as `arknights_assets`.
V53: drop-rate FACT carries expiry + attribution. `stage_drops` row ! `fetched_at` + `expires_at` + penguin `snapshot_id` (provenance) + region (§V17). served past `expires_at` → typed `data_stale` limitation + suggested admin re-sync (§V24); ⊥ present expired as fresh. penguin = CC BY-NC 4.0 ∴ attribution surfaced in `get_data_sources` + `NOTICE`; NC ∴ noncommercial-only (aligns D12; imported data separately governed §V16).
V54: drop rate = penguin-sourced FACT (own provenance chain), distinct from `arknights_assets` game-data FACT; ⊥ silently merge 2 provenance chains in 1 envelope (§V5/§V6 tier discipline). penguin server → region map: CN penguin→cn, US/Global penguin→en; JP/KR penguin servers ∉ {en,cn} v0.2 ∴ dropped, ⊥ mislabel as en/cn.
V55: farming-efficiency = deterministic OBSERVATION (§V6): `rule_id` + evidence (stage `sanity_cost` + drop rate + sample size) + confidence + limitations + `analyzer_version`. computed on typed numeric fields only (§V26), ⊥ NL. drop-sample below floor → reduce confidence + limitation (extends §V8); ⊥ "best farm" mandatory | universal-best label (§V7). expired drop cache (§V53) → efficiency downgraded to limitation, ⊥ fresh recommendation.
V56: announcement importer metadata-ONLY (extends §V16): store `announce_id` + title (name string, capped+sanitized §V18) + date + url + category + region; ⊥ body | html | prose | image | image-url. adapter ENABLED by default (M9 policy review satisfied the D14 gate: T93 recorded `last_reviewed`=2026-07-21 + permission past pending); metadata-only scope = PERMANENT ceiling regardless of enabled state, ⊥ widen w/o new review. announcement region ∈ {en,cn}, ⊥ mixed (§V5). full announcement BODY ⊥ ever (§V16 release-artifact + runtime store).
V57: extra-locale alias = NAME-only + `locale` tag (extends D6/§V18): store canonical name/alias per locale (jp/kr v0.2); ⊥ machine-translated description | prose (D6 no bulk MT). alias `locale` ≠ entity fact region — a cn|en entity ? carry jp/kr NAME alias; alias match returns entity's OWN region facts (§V5 preserved, ⊥ region widen). `entity_fts` carries locale-tagged aliases (single §V37 rebuild home, `GROUP_CONCAT ... ORDER BY` per B22); `search_entities` `locale` param additive (§V21) + multi-locale match ⊥ widen region availability (§V50). `locale` FILTER domain = extra-locale tags ONLY (`ja`|`ko` = `EXTRA_LOCALE_FOR_REGION` values), ⊥ fact-region locales (`en`/`zh`): a fact-region-locale filter is degenerate (an en|cn entity's OWN canonical name IS its region's locale ∴ `locale=en` ≈ `server=en`, redundant w/ the §V5 region gate) AND asymmetric-broken — only operators carry a self-name alias row (T98 stamps `REGION_TO_NAME_LOCALE`), the primary enemy importer inserts NONE ∴ `locale=en` silently keeps operators + drops EVERY enemy. locale axis is a query filter ONLY where locale ⊥ fact region — the jp/kr NAME aliases. ∴ `SearchLocale`={`ja`,`ko`}; the stored en/zh alias-locale tag stays (documents the string's language, feeds §V59 `name_i18n`) but is ⊥ a filter value (B50).
V58: penguin drops = OPTIONAL ride-along in `sync` (`penguin_statistics` ∈ `[sync].enabled_sources` → fetched AFTER game data, imported into the SAME candidate BEFORE validate/promote ∴ drops JOIN the freshly-imported stages/items in ONE atomic build, §V4/§V52). MUST ⊥ compromise §V3 core fail-closed: a penguin fetch|import failure (network | importer `ImporterError` incl its OWN §V30 non-empty-or-fail | duplicate collision) → caught + warned + drops SKIPPED, build CONTINUES game-data-only (empty drops domain legitimate — `items`/`stage_drops` ∉ CRITICAL_TABLES per 0009 ∴ ⊥ a §V30 combat regression). per-server ALL-OR-NOTHING: a penguin error rolls back THAT region's partial drop inserts (savepoint), ⊥ a half-written drop set on the promoted candidate. region→penguin-server = inverse §V54 map (en→US, cn→CN); a region w/o a penguin server skips silently (⊥ jp/kr in en/cn sync). drops that DO import still carry own provenance chain + expiry + region (§V53/§V54); game-data §V3/§V30 fail-closed for the COMBAT domain unchanged.
V59: penguin item `display_name` = REGION-locale name, ⊥ canonical `name` read blind. penguin top-level `name` = canonical CHINESE label; per-locale strings ∈ `name_i18n` (en/zh/ja/ko). `parse_items` ! pick `name_i18n.<locale>` per region-locale map (en→`en`, cn→`zh`), fallback canonical `name` only when locale entry absent | blank ∴ en build surfaces English item names, ⊥ cn text under an en region envelope (§V5 region integrity extends to display strings, ⊥ just fact region). `name_i18n` ∈ `ITEM_ALLOWLIST` = NAME-only per-locale dict (like §V57 alias), ⊥ prose (§V18). (B46)
V60: item→stage drop COMPARISON (`get_item_drops`) = REVERSE of §V54 stage→drops, SAME penguin FACT + provenance chain (§V54), NEW READ only (⊥ new import|migration — reuses `idx_stage_drops_item`, 0009 baris 51 built FOR this reverse lookup). resolve item by (server, game_id)→`items.item_pk`; `drops_for_item` joins `stage_drops`→`stages` for each stage's `sanity_cost`+stage_code+region+provenance. efficiency = REUSE §T90/§V55 `analyze_farming` core `sanity_cost÷drop_rate` at item-across-stage granularity (§V37, ⊥ 2nd math home), RANKED ascending (lowest sanity/item first). ranked comparison ⊥ "best farm"|mandatory|universal-best (§V7/§V55) — ordering + evidence, decision stays user's. limitations MANDATORY: stage availability (event ⊥ permanent — drop cache ⊥ model open/closed) + first-clear bonus + byproduct/synthesis-recipe NOT modeled; expired drop (§V53) downgrades THAT stage's figure to limitation, ⊥ drops it from the list. region integrity (§V5): one item's comparison ⊥ mix en+cn stages — item resolved PER region, only same-region stages ranked. absent item | item w/ 0 drop cache → typed not_found + suggested admin action (§V24). same deterministic typed-field OBSERVATION discipline as §V55.
V61: announcement feed field-MAP (§V56/§T95). real official feed VERIFIED 2026-07-21 vs live (cn `https://ak-conf.hypergryph.com/config/prod/announce_meta/Android/announcement.meta.json`, en `https://ark-us-static-online.yo-star.com/announce/Android/announcement.meta.json`; both `announceList`-wrapped, §V5 per-region host distinct) carries `announceId`+`title`+`day`+`month`+`webUrl`+`group` — NOT the T95 `ANNOUNCEMENT_ALLOWLIST`/`parse_announcements` keys `date`/`url`/`category`. wiring `feed_url` BLIND → only `announceId`+`title` survive allowlist ∴ rows insert ∴ §V30 silent-empty guard ⊥ trip, yet date=url=category all NULL → T96 since/until filter silently broken (degraded build worse than inert). ∴ importer ! field-MAP: `ANNOUNCEMENT_ALLOWLIST` += `day`,`month`,`webUrl`,`group` (int/enum/name string, ⊥ prose §V18; metadata-only §V56 ceiling intact — ⊥ body/html/image); `parse_announcements` normalize `webUrl`→url, `group`→category, `day`+`month`→date ISO. feed carries NO year ∴ year INFERRED by NEAREST: pick whichever of prior/current/next year places `month`+`day` CLOSEST to `fetched_at` — a pre-announced near-future month stays current year, an entry just past the Dec→Jan boundary rolls back, UTC-vs-server New-Year skew resolves to the nearest real date; NOT a blind "entry month > fetch month → prior year" which mislabels every pre-announced maintenance a year early + flips year on the UTC boundary (B47). calendar-invalid `day`/`month` (2/31, 4/31, 2/29 off a leap yr) constructed via `datetime.date` → `None`, ⊥ a fabricated impossible date string (§V26). §V30 EXTENDED to degradation: non-empty feed → rows w/ `date` all-NULL ∀ rows = silent-degraded build → fail closed (⊥ only missing-announceId). guard keys on `date` ALONE, NOT date=url=category all-3-null: the since/until filter (§T96) keys on `date` only ∴ a break nulling just `date` while url/category still map = a silently-empty window (the exact §V61 trap); guarding `date` alone is strictly stronger (an all-3-null set already has date all-null) + closes that gap (B47). url/category ⊥ filter keys ∴ their absence degrades VISIBLY, ⊥ a guard trip. allowlist change → `FIELD_POLICY_VERSION` bump. same real-shape-≠-code class as B6/B37/B38.

V62: banner ARCHIVE domain = historical gacha-schedule FACT from primary `arknights_assets_gamedata` `gamedata/excel/gacha_table.json` `gachaPoolClient` (389 EN entries, past + near-future scheduled; ⊥ new source|adapter|registry — SAME snapshot as enemy/stage/operator ∴ fetched via `SUPPLEMENTARY_FILES` §V41/B36, tolerant-absent; §V41 introspection test catches an un-wired file). per pool store: `gachaPoolId`+`gachaPoolName` (display name, capped+sanitized §V18)+`openTime`+`endTime` (unix epoch int → ISO normalize; CLEAN, ⊥ §V61 day/month/year-infer nightmare)+`gachaRuleType`+region. metadata-ONLY (§V16/§V56 ceiling class): ⊥ `gachaPoolSummary`|`gachaPoolDetail`|`dynMeta` prose|html|image. featured-op = TYPED-ONLY, per rule type (VERIFIED vs live EN+CN @2026-07-21): `LIMITED`→`limitParam.limitedCharId` (single char id, 24 EN); {`ATTAIN`,`CLASSIC`,`CLASSIC_ATTAIN`,`CLASSIC_DOUBLE`,`FESCLASSIC`,`SPECIAL`}→`dynMeta.attainRare6CharList` (array, 83 EN); {`NORMAL`,`SINGLE`,`DOUBLE`,`LINKAGE`} carry NO typed featured-op (282/389 EN; CN rate-up = HTML `<@ga.up>` prose ∴ §V18-forbidden, EN `gachaPoolDetail`=`"-"` ∴ absent) → emit NONE + limitation "standard-banner rate-up not in typed gamedata" (§V26 missing-field→limitation; ⊥ fabricate|parse prose). featured char_id → SOFT-resolve to `operator_pk` when operator present, else carry raw char_id + limitation (unresolvable featured-op ⊥ fail build — archive standalone FACT, §V3 preserved; operators optional-zero B36 ∴ combat-only snapshot → raw char_ids). banner region ∈ {en,cn}, ⊥ mixed (§V5). banner domain = OPTIONAL, ISOLATED fail-OPEN (mirrors §V58 penguin): runs INSIDE main build AFTER operators (soft-resolve needs same-candidate `operator_pk` ∴ ⊥ post-promotion ride-along) BUT under per-region `SAVEPOINT banners` — a banner `ImporterError` (§V30 non-empty `gacha_table`→0 banners | §V33 dup `gachaPoolId`/repeated featured char_id collision) → `ROLLBACK TO SAVEPOINT` (drop THAT region's partial banner+featured-op+provenance rows) + warn + banner domain SKIPPED, combat build CONTINUES (banners ∉ CRITICAL_TABLES ∴ empty legit, ⊥ §V30 combat regression). ∴ OPTIONAL archive ⊥ fail-close MANDATORY enemy/stage/operator core (§V3 combat fail-closed UNCHANGED; banner §V30 folded into the fail-open catch, matches §V58). archive = FACT (region+provenance §V5/§V17), ⊥ PLANNING (rate/pity/spark constants VERIFIED absent from `gacha_table` §C ∴ deferred). allowlist change → `FIELD_POLICY_VERSION` bump.

## §T TASKS

id|status|task|cites
T87|x|M8 enable `penguin_statistics` source: `sources/penguin_statistics.py` adapter (allowlist, HTTPS, size/depth/count/redirect limits, CLI-only fetch) + registry entry (owner/URL/purpose/regions/CC BY-NC 4.0/attribution/last_reviewed) + `DATA_SOURCES.md` + `NOTICE` NC attribution|V52,V27,V53,C
T88|x|M8 migration 0009 `items` + `stage_drops` (item_id, stage_id, quantity, times, drop_rate, `fetched_at`, `expires_at`, penguin `snapshot_id`, region, provenance FK)|V17,V53
T89|x|M8 penguin importer `importers/penguin_drops.py` → items + stage_drops; field allowlist + recursive sanitize; penguin server→region map (US/Global→en, CN→cn; jp/kr dropped); stamp expiry + provenance; §V30 non-empty-or-fail|V17,V18,V53,V54,V30
T90|x|M8 farming-efficiency analyzer `analyzers/farming.py` + rule(s): expected `sanity_cost`÷drop_rate → sanity/item; sample-size floor → reduce conf + limitation; deterministic typed-field evidence; ⊥ mandatory/best-farm label|V6,V8,V26,V55,V7
T91|x|M8 `get_stage_drops` MCP tool (drop facts + region + penguin provenance + expiry; `include_efficiency` flag → farming observations; past-`expires_at`→`data_stale` limitation); typed envelope; register in `_TOOL_BUILDERS` (both transports)|V5,V21,V22,V23,V52,V53,V55,V14,I.tool
T92|x|M8 golden/accept: pinned penguin snapshot fixture (⊥ commit bulk data, §V16) → drops + attribution + expiry; expired cache→`data_stale`; farming efficiency obs w/ confidence+limitation; en/cn ⊥ mixed|V5,V16,V53,V55
T93|x|M9 source policy review → `DATA_SOURCES.md` + registry entry for announcement source (metadata-only, disabled-by-default, `last_reviewed`, D14)|V27,V56
T94|x|M9 migration 0010 `announcements` (announce_id, title, date, url, category, region, provenance FK)|V17,V56
T95|x|M9 announcement adapter `sources/announcements.py` + importer `importers/announcements.py` metadata-ONLY (announce_id/title/date/url/category); ⊥ body/html/prose/image; recursive allowlist+cap+sanitize; region en/cn; §V30 guard|V16,V18,V56,V30
T96|x|M9 `get_announcements` MCP tool (metadata list, region, optional since/until date filter, bounded pagination); typed envelope; ⊥ prose; register both transports|V5,V19,V22,V23,V56,V14,I.tool
T97|x|M9 accept: enabled announcement source → metadata rows (no body); disabled-by-default gate honored; en/cn separation; `get_data_sources` shows attribution + `last_reviewed`|V16,V27,V56,V5
T98|x|M10 migration 0011 add `locale` col to alias table (+ index); backfill existing en/cn aliases w/ locale tag|V17,V57
T99|x|M10 extra-locale alias source + importer (jp/kr canonical NAMES only, ⊥ MT prose/description); locale-tagged rows; entity fact region unchanged|V18,V57,V30
T100|x|M10 rebuild `entity_fts` w/ locale-tagged aliases (single §V37 home, `GROUP_CONCAT ... ORDER BY` per B22); `search_entities` additive `locale` param + multi-locale match ⊥ widen region availability|V37,V50,V57,V21
T101|x|M10 accept: jp/kr alias search resolves entity → returns entity's OWN region facts (⊥ region mix); alias locale ≠ fact region; ⊥ translated prose stored|V5,V57,V16
T103|x|M8 item→stage drop comparison read layer (⊥ new import|migration, reuses `idx_stage_drops_item`): `DropRepository.drops_for_item(item_pk)` joins `stage_drops`→`stages` (stage `sanity_cost`+stage_code+region+provenance); resolve item by (server, game_id); `services/drops.get_item_drops` REUSES §T90 `analyze_farming` `sanity_cost÷drop_rate` core (§V37, ⊥ 2nd math home) → RANKED asc; absent item|0 cache→not_found+suggested action; region-scoped (⊥ mix en+cn); mandatory limitations (stage availability/first-clear/byproduct ⊥ modeled); expired→limitation ⊥ dropped|V60,V54,V55,V37,V5,V24,V7
T104|x|M8 `get_item_drops` MCP tool (item drop-across-stages facts + per-stage penguin provenance + expiry; `include_efficiency`→ranked farming observations; past-`expires_at`→`data_stale`; `stages_page`/`efficiency_page` bound the two growable lists per §V22/§V19 — the reverse item→stage view is unbounded (common material ~100-200 stages), ranking/stale/provenance computed over FULL set BEFORE slicing so page 1 = most-efficient N + `data_stale` holds w/ off-page expiry, B21); typed envelope; register `_TOOL_BUILDERS` both transports|V60,V21,V22,V23,V52,V53,V55,V19,V14,I.tool
T105|x|M8 accept: `get_item_drops` ranked asc by sanity/item over ≥2 stages; ⊥ "best farm" label; en/cn item comparison ⊥ mixed; expired stage→limitation not dropped; absent item→not_found|V60,V5,V55,V7,V24
T102|x|M8 wire penguin ride-along into `sync` (closes T87-T92 gap: importer built + tested but never called ∴ `get_stage_drops` inert, penguin `enabled=false`): `penguin_statistics` ∈ `[sync].enabled_sources` + registry-enabled → AFTER game-data import, per region fetch `PenguinStatsAdapter` (region→penguin-server = inverse §V54: en→US, cn→CN; region w/o server skipped) + `import_penguin_drops` into the SAME candidate under a per-server SAVEPOINT before validate/promote; penguin fetch|import failure → rollback THAT region's savepoint + warn + CONTINUE game-data-only (§V58 fail-open, ⊥ break §V3 core). config `[sync.penguin_statistics].base_url` (default `PENGUIN_BASE_URL`) folded via existing `SyncSourceConfig`; report per-region drops_inserted/drops_skipped. accept: enabled penguin (fake fetcher) → drops in PROMOTED db + `get_stage_drops` returns facts+efficiency; penguin outage → game-data-only build STILL promotes (drops empty, ⊥ fail); disabled penguin → ⊥ fetched|V58,V52,V3,V30,V53,V54,I.tool
T106|x|M9 follow-through on §V56 enabled-by-default flip: (1) registry `enabled=true` for both `arknights_global_official_news`+`arknights_cn_official_news` in `config/data_sources.toml` (+ `data_sources.example.toml`) — passes `assert_complete` (`license_identifier` ∉ mandatory ∴ empty OK; all §V27 mandatory fields populated by T93); (2) wire announcement ride-along into `sync` — CURRENTLY INERT: `import_announcements`/`AnnouncementsAdapter` built+tested (T95/T96) but ⊥ called by any CLI ∴ `get_announcements` empty on a synced db. mirror T102 penguin: `[sync].enabled_sources` gate + `[sync.<source_id>].feed_url` (official feed URL TBD, blocker) + per-region `AnnouncementsAdapter`→`import_announcements` under per-server SAVEPOINT AFTER game-data import, fail-open (a feed/import failure → rollback+warn+CONTINUE game-data-only, ⊥ break §V3 core; announcements ∉ CRITICAL_TABLES); (3) flip `enabled is False`→`True` asserts + rename `test_disabled_by_default_gate_honored`→enabled (test_announcement_source_review, test_accept_announcements). accept: enabled `sync` (fake fetcher)→announcement metadata rows in PROMOTED db + `get_announcements` returns facts; feed outage→game-data-only build STILL promotes|V56,V27,V58,V3,V5,I.tool
T107|x|M9 announcement feed field-MAP (unblocks T106 `feed_url`, §V61): real official feed VERIFIED 2026-07-21 (cn `https://ak-conf.hypergryph.com/config/prod/announce_meta/Android/announcement.meta.json`, en `https://ark-us-static-online.yo-star.com/announce/Android/announcement.meta.json` — en URL from client `network_config` key `an`; §V5 per-region host distinct) carries `announceId`+`title`+`day`+`month`+`webUrl`+`group` ≠ T95 keys `date`/`url`/`category`. (1) `ANNOUNCEMENT_ALLOWLIST` += `day`,`month`,`webUrl`,`group` (`field_policy.py`; int/enum/name-string ⊥ prose §V18; metadata-only §V56 ceiling intact — ⊥ body/html/image) + bump `FIELD_POLICY_VERSION`; (2) `parse_announcements` normalize `webUrl`→url, `group`→category, `day`+`month`→date ISO (year=`fetched_at` year + Dec→Jan rollover guard: entry month > fetch month → prior year); (3) §V30 EXTEND: non-empty feed → rows w/ date=url=category all-NULL ∀ rows = silent-degraded → fail closed (⊥ only missing-`announceId`); (4) record verified URLs in `config/data_sources.toml` (existing T93 source ids) + `DATA_SOURCES.md`; (5) set `config.toml` `[sync].enabled_sources` + `[sync.<id>].feed_url` per region. accept: real-shape entry → non-null date/url/category; all-NULL-mapped feed → `ImporterError` (⊥ silent degraded); en/cn ⊥ mixed|V61,V56,V18,V30,V16
T108|x|M10 review backprop — apply B50+B51 (B52 `if name:` already landed this session). (1) B50: restrict `SearchLocale`→`Literal["ja","ko"]` (`models/common.py`) — `en`/`zh` filter values were degenerate (an entity's own name IS its region locale ∴ ≈ `server=`) + asymmetric-broken (enemies carry NO self-alias ∴ `locale=en` silently dropped every enemy); retarget `test_search_locale_domain_matches_field_policy_maps` to `EXTRA_LOCALE_FOR_REGION.values()` ONLY + add a `locale="en"`→protocol `ValidationError` test. (2) B51: `import_locale_aliases` §V30 guard PER DOMAIN keyed on MATCHED game_ids (⊥ combined insert count) — `_insert_locale_aliases` also returns a matched-pk count; `op_names non-empty and op_matched==0` OR enemy equiv → `ImporterError` naming the missed domain; test a source matching operators but 0 enemies fails closed. accept: `locale=en`/`zh` rejected at model gate; a per-domain game_id-scheme mismatch → `ImporterError` ⊥ silent drop|V57,V50,V30
T109|x|M10 wire extra-locale ride-along into `sync` (closes T99 gap: `ExtraLocaleAliasAdapter`+`import_locale_aliases` built+tested but ⊥ called by any CLI ∴ `search_entities(locale=ja/ko)` empty on a synced db + no jp/kr NAME searchable — same inert class as T102/T106). mirror T102/T106: registry source id `arknights_extra_locale_names` + `[sync].enabled_sources` gate + `[sync.<id>].base_url` per extra locale (jp/kr) → AFTER game-data import, per extra-locale fetch `ExtraLocaleAliasAdapter`→`import_locale_aliases` onto the SAME candidate under a per-locale SAVEPOINT BEFORE validate/promote, THEN `rebuild_search_index` so the new aliases enter FTS; fail-open (a fetch|import failure → rollback THAT locale's savepoint + warn + CONTINUE game-data-only, ⊥ break §V3; alias tables ∉ CRITICAL_TABLES). idempotency (review #4): migration 0012 `UNIQUE(operator_pk, alias, locale)` + `UNIQUE(enemy_pk, alias, locale)` + `_insert_locale_aliases` → `INSERT OR IGNORE` ∴ a re-run/backfill ⊥ double-insert (⊥ a dup alias in the FTS `GROUP_CONCAT`); the B51 MATCHED-not-inserted guard stays correct under OR IGNORE (a re-run MATCHES the game_ids even when the INSERT is IGNORED). accept: enabled extra-locale sync (fake fetcher)→jp/kr aliases in PROMOTED db + `search_entities(locale=ja)` resolves; extra-locale outage→game-data-only build STILL promotes; disabled→⊥ fetched; re-run sync→⊥ duplicate alias rows|V57,V58,V3,V30,V37,I.tool
T110|x|M11 ADR `docs/adr/` banner archive: D5 "banners deferred" → carve banner ARCHIVE (historical FACT) IN, PLANNING (pull/spark/rate-pity/shop) STAYS out; cite roadmap §6.3, §V62, verified `gacha_table` shape (typed featured-op LIMITED+CLASSIC-family only, rate/pity/spark constants ABSENT); same carve pattern as penguin/announcement v0.2 enables|V62,C
T111|x|M11 banner allowlist + fetch wiring: `BANNER_ALLOWLIST` (`field_policy.py`: `gachaPoolId`,`gachaPoolName`,`openTime`,`endTime`,`gachaRuleType`,`limitParam.limitedCharId`,`dynMeta.attainRare6CharList` — TYPED only; ⊥ `gachaPoolSummary`|`gachaPoolDetail`|prose §V18/§V56) + bump `FIELD_POLICY_VERSION`; add `gamedata/excel/gacha_table.json` to `arknights_assets.SUPPLEMENTARY_FILES` (fetched every sync via `_download_optional`, tolerant 404/410 skip+warn §V41/B36; introspection test asserts banner importer default path ⊆ staged set)|V62,V18,V41,V16
T112|x|M11 migration 0013 `banners` (banner_pk, server, game_id=`gachaPoolId`, display_name, open_time, end_time, rule_type, region, provenance FK) + child `banner_featured_ops` (banner_pk FK, operator_pk NULL-able FK, char_id, resolved flag) for typed featured-op (LIMITED single + CLASSIC-family array); index `(server, open_time)`|V17,V62
T113|x|M11 banner importer `importers/banners.py` metadata-ONLY (parse `gachaPoolClient`; `openTime`/`endTime` unix epoch→ISO; featured-op TYPED per rule-type §V62: `LIMITED`→`limitedCharId`, {`ATTAIN`,`CLASSIC`,`CLASSIC_ATTAIN`,`CLASSIC_DOUBLE`,`FESCLASSIC`,`SPECIAL`}→`attainRare6CharList`, {`NORMAL`,`SINGLE`,`DOUBLE`,`LINKAGE`}→NONE; char_id SOFT-resolve→`operator_pk` else raw+resolved=false, unresolvable ⊥ fail §V3; recursive allowlist+cap+sanitize §V18/V31; §V30 non-empty `gacha_table`→0 banners fail-closed) + WIRE into MAIN game-data import pipeline AFTER `import_operators` (soft-resolve needs operators present; ⊥ ride-along — SAME snapshot as combat data, tolerant-absent like operators B36); purge cascade §V32|V62,V16,V18,V30,V31,V3,V32,V37
T114|x|M11 `get_banners` MCP tool (banner facts + region + typed featured-op + provenance; standard-banner→"rate-up not in typed gamedata" limitation §V62/§V26; optional since/until ISO-validated date filter reusing §T96/B49 §V19; bounded pagination §V22; typed envelope §V23) + `arknights://banners/{server}` resource; register `_TOOL_BUILDERS` both transports §V14|V62,V5,V19,V22,V23,V26,V14,I.tool
T115|x|M11 accept: synced db → banner metadata rows (⊥ prose/html); LIMITED→`limitedCharId` resolved to operator, CLASSIC-family→`attainRare6CharList`, NORMAL→no featured-op + limitation; en/cn ⊥ mixed; absent `gacha_table`→0 banners tolerant (⊥ fail); non-empty→0 banners fail-closed; `get_banners` since/until filter; `get_data_sources` unchanged (⊥ new source — reuses `arknights_assets_gamedata`)|V62,V5,V30,V16,V7
T116|x|M11 banner fail-open ISOLATION (B53): wrap `import_banners` call in `_import_one` (`importers/pipeline.py`) under fixed `SAVEPOINT banners` (regions sequential ∴ 1 reused name, mirrors `_ride_along` fixed `ride_along` name) + fail-open catch — a banner `ImporterError` (§V30 non-empty→0 | §V33 dup `gachaPoolId`/repeated featured char_id) → `ROLLBACK TO SAVEPOINT banners` (drop THAT region's partial banner+featured-op+provenance rows) + warn + banner SKIPPED, combat build CONTINUES; `RELEASE` on success. banners STAYS in main build (operator_pk soft-resolve needs operators same candidate, ⊥ ride-along) but savepoint-isolated ∴ optional archive ⊥ nuke mandatory combat core (§V3 combat fail-closed unchanged). extract shared main-build-savepoint helper IFF it dedups vs `_ride_along` (§V37). accept: `gacha_table` w/ dup `attainRare6CharList` char (or dup `gachaPoolId`) → 0 banners + WARN + enemies/stages/operators STILL promoted (⊥ whole-build abort)|V62,V58,V33,V30,V3,V37

id|date|cause|fix
B1|2026-07-17|V5: `sync` reused 1 region-agnostic `base_url` ∀ server → en+cn fetch identical bytes labeled diff region; validation passes on mislabeled data|per-region `base_url_for(server)` (`{server}` token / `base_urls` map) + `_cmd_sync` guard refuses if 2 servers resolve same URL
B2|2026-07-17|V1/PRD17.4: `max_total_download_mb` loaded but never wired → adapter always used hardcoded 512 MiB; operator cap dead|`_download_limits(config)` builds `DownloadLimits(max_total_bytes=mb*1024*1024)`, passed to adapter
B3|2026-07-17|V1/PRD17.4: redirect handler checked HTTPS+count only, no same-domain → 302 to foreign host followed, domain allowlist bypassed|`_BoundedRedirectHandler` default-deny cross-domain vs original host; `allow_cross_domain` opt-out
B4|2026-07-17|V1/PRD17.4: `_total_bytes` per-adapter + fresh adapter per server → `sync --server all` allowed ~2x total cap|shared run-level `DownloadBudget` injected into all adapters in the run
B5|2026-07-17|`json.loads` ran before depth cap → deep-nested JSON raised uncaught `RecursionError` traceback not graceful reject|catch `RecursionError` in `_download` → `SourceAdapterError("JSON exceeds safe nesting depth")`
B6|2026-07-17|H1 verified vs upstream `master`@`413a81a3ff3e`: T13/T14/T21 parsers+fixture `tests/fixtures/stage_4_4` target synthetic shape ≠ real `arknights_assets_gamedata`. (a) real `enemy_database.json` = top-level id-keyed dict of list `{level, enemyData.attributes.<stat>.m_value}`, no `"enemies"` wrapper → `parse_enemies` guard `"enemies" not in database_raw` raises `ImporterError` (in `_HANDLED_ERRORS` → graceful exit 1) at once; stat keys differ (`maxHp`≠`hp`, `magicResistance`≠`res`, `baseAttackTime`≠`attackInterval`). (b) real `levelId`=`Obt/Main/level_main_04-04` (Title-case, no `gamedata/levels/` prefix, no `.json`) → `_discover_level_paths` collects 0/3264 → 0 level files. (c) real level `mapData` no width/height (grid `map`), tiles no x/y → 0/117 kept; wave action enemy under `key` ≠ `enemyId` → 0/26 spawns. (d) `motionType` absent from handbook (real motion @ `enemy_database` `enemyData.motion.m_value`) → aerial rule V6 substrate empty. ∴ real `sync --server en` ⊥ import combat data (halts at enemy-DB guard; if bypassed → silent empty stages). NOT H1's predicted raw `AttributeError`|V29,V30 ∴ T66 normalization transform + T67 real-shape contract test; T13/T14/T21 ⊥ done vs real data (fixture-only)
B7|2026-07-17|V17: level-file-derived rows (stage_maps/tiles/routes/waves/spawns/stage_enemies) written by `insert_level` w/o `make_record_provenance`, tables had no `provenance_id` col → threat-analysis substrate carried no per-record provenance/tamper hash (snapshot `manifest_hash` covers bytes, not per-record)|migration `0006_provenance_backfill.sql` adds `provenance_id` FK on level+zone tables; `stages.py` creates a per-level-file provenance row; `insert_level` stamps every row (test_level_rows_carry_provenance, test_zone_carries_provenance)
B8|2026-07-17|V18: `apply_allowlist` sanitized only top-level `str`; kept dict/list values (abilities, immunities, environment, specialProperties, route positions) stored raw as JSON → nested control/bidi chars survived + escaped the length cap, reaching `*_json` cols exposed to the LLM client|`field_policy.sanitize_value()` recurses over nested string leaves, routed by `apply_allowlist`+`levels.py` (test_allowlist_sanitizes_nested_string_leaves) ∴ V31
B9|2026-07-17|V18: `stage_spawns.source_fragment_json` stored the entire raw wave action → any prose/injection field in the source action was JSON-dumped in, contra the known-keys-only fragment contract|`SPAWN_ACTION_ALLOWLIST`; `levels.py` stores the allowlisted fragment only (test_spawn_source_fragment_is_allowlisted) ∴ V31
B10|2026-07-17|V20: `_purge_source_rows` deleted `record_provenance` but cascaded only enemy/stage children; operators/skills/modules declare `provenance_id NOT NULL REFERENCES record_provenance` w/o ON DELETE CASCADE → the delete raised `IntegrityError`, purge aborted, a source w/ operators was never purgeable (latent: M1 operator importers stub)|`db/purge.py` cascades operator/skill/module/talent before `record_provenance`; `IntegrityError`→`PurgeError` fail-closed (test_purge_cascades_operator_domain) ∴ V32
B11|2026-07-17|V20: purge `append_event(purge)` was journaled to `policy_events.jsonl` before `purge_and_rebuild` → a failed-validation rebuild left a durable phantom purge; the next sync `materialize_policy_events` wrote it into the build while the source rows were still present|`_cmd_source_purge` builds the event in memory, journals it durably only after validate+promote (test_source_purge_failed_validation_no_phantom_purge) ∴ V32
B12|2026-07-17|V20: purge flipped `data_sources.enabled=0` only inside the rebuilt build; the machine-registry TOML that gates `_cmd_sync` was untouched → `purge X` w/o a prior `disable X` was repopulated by the next sync; registry vs build enabled-state diverged|`_cmd_source_purge` also `set_source_enabled(id, False)` + journals the disable at once (test_source_purge_also_disables_registry) ∴ V32
B13|2026-07-17|`enemy_levels UNIQUE(enemy_pk, level_variant)` (+ stage_waves/routes/tiles uniqueness): a repeated or absent index in the source (`_as_int(...) or 0` → two missing `level` collide at variant 0) raised an uncaught `IntegrityError` that tore down the whole multi-region candidate build|`enemies.py`+`levels.py` catch `sqlite3.IntegrityError`→`ImporterError` (test_duplicate_level_variant_fails_gracefully) ∴ V33
B14|2026-07-17|V6: aerial rule `_summary(len(evidence))` counted one `EvidenceItem` per (enemy, level_variant); a flyer at 2 variants reported "2 aerial enemy types" for 1 enemy + duplicate evidence refs|`analyzers/rules/aerial.py` counts distinct `evidence.ref` (test_same_flyer_at_two_variants_counts_as_one_type) ∴ V35
B15|2026-07-17|V27: two divergent public projections — `registry.public_view()` dropped `private_hosting_status` via `_INTERNAL_ONLY_FIELDS` but the `get_data_sources` service included it + bypassed `public_view()` → `source list` and the MCP tool emitted different field sets; the allowlist was maintained in 2 places|single `_INTERNAL_ONLY_FIELDS={"policy_notes"}` projection (private_hosting_status intended-public, PRD §13.10); `get_data_sources` routes through it (test_public_projections_do_not_diverge) ∴ V34
B16|2026-07-17|V16/D4: `DEFAULT_MIGRATIONS_DIR = parents[3]/"migrations"` resolved to the repo root, outside the packaged `src/arknights_mcp` tree → a non-editable install found 0 `.sql` files and `build_database`/`_expected_schema_version` broke (unexercised: T47 install-smoke not done)|moved `migrations/*.sql`→`src/arknights_mcp/migrations/`; `db/migrations.py` resolves via `importlib.resources` (ships in wheel); enforcing test deferred to T47
B17|2026-07-18|T66 normalizes `levelId` (real `Obt/Main/level_main_04-04`→`gamedata/levels/obt/main/level_main_04-04.json`) ∴ real level files fetch; but old `_discover_level_paths` L8 gate accepted a levelId only if its *raw* value startswith `gamedata/levels/` → real Title-case ⊥ prefix → collected 0/3264 files (B6b), & post-normalize every levelId maps under `gamedata/levels/` ∴ raw-prefix check ⊥ valid L8 boundary; excel craft `gamedata/excel/character_table.json` + traversal must reject *after* normalize|`_discover_level_paths` confines the *normalized* path to a clean level path (under `gamedata/levels/`+`.json`, ⊥ `.`/`..` seg, ⊥ nested `gamedata`/`excel` seg); `normalize_level_id` always forces the `gamedata/levels/` prefix ∴ V36
B18|2026-07-18|V34/V27 (B15 fix incomplete): `get_data_sources` builds service `SourceInfo` by own field enumeration, ⊥ routes through `registry.public_view()` drop-list → `source list --json` emits `adapter_version`+`transform_version` but MCP tool omits both = 2 divergent public allowlists again; `test_public_projections_do_not_diverge` asserts only `policy_notes`-disjoint + 3 named fields, ⊥ set-equality ∴ blind to the re-fork|route `get_data_sources`/`SourceInfo` through the single `registry.public_view()` projection ∴ both surfaces emit identical keys (minus DB-only `active_snapshots`); strengthen test → set-equality assert; §V34 tightened ∴ T74
B19|2026-07-18|V32/V16/V20: `_purge_source_rows` DELETEs base rows but `entity_fts` is a standalone FTS5 index w/ no triggers (0007) → the purged source's game_id/name/alias docs survive in the index ∴ `search_entities(server=purged)` still returns the taken-down entity + `get_*` 404s on the dangling game_id (V16 leak); `purge_and_rebuild`'s filtered copy left the promoted index stale|`db/purge.py` calls `rebuild_search_index(conn)` (DELETE FROM entity_fts + repopulate from surviving rows, single §V37 home) inside the purge txn; drill asserts en FTS docs gone + en search empty while cn stays live (test_takedown_drill_purges_only_target_keeps_others_live) ∴ V32
B20|2026-07-18|V4: `PRAGMA integrity_check` ⊥ verify FTS5 shadow-table consistency + `_fts_smoke` only ran a no-match MATCH (returns 0, always passes) → a stale/corrupt FTS index passed the promotion gate|`_fts_smoke` runs FTS5's own `INSERT INTO entity_fts(entity_fts) VALUES('integrity-check')`; prepared as a write ∴ short-lived `mode=rw` handle (performs no writes → candidate byte-identical, T24) (test_corrupt_fts_index_detected) ∴ V4
B21|2026-07-20|V22/V19: `get_item_drops` (T104) cited §V22 but shipped NO page bound while its sibling `get_stage`/`GetStageInput` pages every heavy section; the reverse item→stage view is the one drop list that grows unbounded (a common T2/T3 material drops across ~100-200 permanent stages, ~1KB/stage w/ `include_efficiency`) → a frequently-dropped item overflowed `MAX_RESPONSE_BYTES` + `_enforce_cap` failed the WHOLE response closed to `partial data={}` advising "pagination" the input model never exposed ∴ zero data + no knob to recover|`GetItemDropsInput` gains `stages_page`/`efficiency_page` (`PageParams`); `get_item_drops` validates both via the shared `_validate_page` (§V19 dual-enforce, ⊥ silent clamp) then ranks/stale/provenance over the FULL set BEFORE slicing each list (global ranking preserved: page 1 = most-efficient N; `data_stale` holds even when the expired stage is off-page); provenance derivation moved into the service (over full set, dedup on `(snapshot_id,imported_at)`, server=item region) so a later page never drops a snapshot; `page_to_dict`→`_shared.py` single §V37 home shared w/ `get_stage`; default `page_size=50` ∴ small items unchanged (page 1, `has_more=false`) (test_stages_are_paged/test_efficiency_observations_are_paged_over_global_ranking/test_stale_holds_when_expired_stage_off_page) ∴ V22,V19
B21|2026-07-18|V22: `serialized_size` measured w/ `ensure_ascii=False` (compact UTF-8) → a CN-heavy envelope ~199KB is ~2x larger once the T51 Streamable-HTTP serializer emits `\uXXXX` escapes (3-byte CJK → 6 ASCII) ∴ envelope passes the cap but exceeds 200KB on the wire|measure w/ `ensure_ascii=True` (JSON default) = worst-case upper bound ≥ any UTF-8 encoding ∴ cap holds on the wire ∀ transport encoding; fail-closed (over-measure, ⊥ under) ∴ V22
B22|2026-07-18|T24/V14: `GROUP_CONCAT(a.alias, ' ')` (enemy+operator alias agg in `search_index.py`) had no ORDER BY; SQLite concat order is arbitrary → two builds of byte-identical source could emit aliases in different order → FTS doc bytes differ → file `database_hash` differs → T24 "unchanged→no-op" treats identical data as changed + byte-reproducibility broken|`GROUP_CONCAT(a.alias, ' ' ORDER BY a.alias)` pins the concat order (SQLite ≥3.44 ordered-aggregate) in `_ENEMY_SQL`/`_OPERATOR_SQL`
B23|2026-07-18|V19: service `_clamp_limit` silently `max(1, min(limit, MAX))` clamped while `SearchEntitiesInput` *rejects* out-of-range (`ge=1, le=50`) → two divergent V19 behaviors; a caller reaching the service directly (or a transport skipping model validation) got a silent widen/narrow ⊥ a rejection|`_validate_limit` raises `ValueError` on out-of-range (mirrors the model) ∴ one V19 contract enforced identically both places (test_out_of_range_limit_rejected) ∴ V19
B24|2026-07-18|V5 (T32-T38 review): `arknights://status/{server}` `_make_status_handler` filtered `snapshots` by region but reused the *global* `DataStatus.status`/`warnings`/`suggested_action` + derived env_status from the global verdict → a region w/ 0 active snapshots (en-only build, query cn) reported `ok` + empty snapshots + no staleness warning ∴ client concludes cn data present+fresh when absent|region-scope the whole verdict: no region snapshot → `data_stale` + region warning/action; override `data.status`/`warnings`/`suggested_action` to the region view (test_status_other_region_has_no_snapshots asserts cn→data_stale) ∴ V5
B25|2026-07-18|V19 (T32-T38 review): `get_stage` single `page`/`page_size` cursor computed one `offset` applied to tiles+routes+spawns together → a multi-section call could not page one large section without shifting the smaller ones off (page 2 for tiles returned `routes:[]`/`spawns:[]` as if none exist); sections coupled to one cursor|per-section cursors `map_page`/`routes_page`/`spawns_page` (each bounded `PageParams`), independent offset + `*_page` descriptor ∴ hold whole stage in one call yet page a large section alone (test_sections_page_independently) ∴ V19
B26|2026-07-18|V19 (T32-T38 review): `_SPAWN_COUNT_SQL` JOINed only `stage_waves` while `_SPAWNS_SQL` also INNER JOIN `enemies` → a spawn w/ an absent enemy row (SQLite ⊥ enforce FK w/o `PRAGMA foreign_keys=ON`) is counted but not returned ∴ `total`>rows, `has_more` stuck true on the last real page → a paging client loops on a never-filling page|`_SPAWN_COUNT_SQL` mirrors the `enemies` join exactly ∴ count = returnable rows ∴ V19
B27|2026-07-18|V22 (T32-T38 review): `get_stage(include_map=True)` on a stage w/ no `stage_maps` row still built an all-null `StageMapFacts` + emitted a `map` object → "map absent" indistinguishable from "map present but empty"|surface the map section only when `raw_map is not None or tile_count>0`, else leave it `None` ∴ the tool emits no `map` key for a genuinely map-less stage ∴ V22
B28|2026-07-18|I.tool (T32-T38 review): `get_stage` nested `tiles`+`tiles_page` inside the `map` object while `routes_page`/`spawns_page` sat at the top level of `data` → asymmetric wire shape, the per-section page descriptors live in 2 different places ∴ a client can't use one uniform `data[section+'_page']` lookup|`tiles` is its own top-level paged section (`tiles`/`tiles_page`) like routes/spawns; `map` is header-only ∴ all 3 heavy sections share one shape (test_include_map_returns_header_and_paged_tiles)
B29|2026-07-18|V37 (T32-T38 review): `services/stages._parse_abilities` re-implemented the try/except `json.loads` decode that T37 centralized in `util.coerce.json_load` → 2 read-side decode homes ∴ a future decode change must be made in both or they silently diverge|`_parse_abilities` decodes through `json_load`, applies only the list/str shaping the analyzer needs on top; dropped the local `import json` ∴ V37
B30|2026-07-19|T39-T46 review (verified vs real 4-4 @`413a81a3ff3e`): `pressure_spike.py:49-62` sets `window = last_spawn_time - first_spawn_time` & flags burst @0.8 conf if `total_count≥6` & `window≤12s`. but upstream `spawnTime` always null → importer falls back to `action.preDelay` (`normalization.py:372`) = fragment-relative offset; wave `preDelay` + fragment `preDelay` dropped by `_normalize_waves`; `stage_enemies.first/last_spawn_time` (`levels.py:312-318`) = min/max of that fragment-relative preDelay across ALL waves ≠ elapsed real time ∴ enemy trickled across ≥6 fragments @low action-`preDelay` collapses window≈0 → false "concentrated burst". real 4-4: `enemy_1012_dcross` window=0 count=4; 2 `nsabr` actions importer sees @spawn_time=3 really @6s(w0)/@23s(w1). NOTE §V29 `preDelay`→`spawnTime` = field-NAME map, ⊥ a time-absolute claim|V39
B31|2026-07-19|V9/V10 (T52 pre-build, verified vs live Auth0 M2M token + Cloudflare Tunnel topology): `config.py:202` `assert_remote_startup_safe` returns early on `remote.is_loopback` + `transports/streamable_http.py:91` allows loopback bind w/ NO auth (loopback = authless dev exception). Cloudflare Tunnel binds server `127.0.0.1:8000` but Cloudflare serves public internet → public endpoint w/ §V9 auth gate OFF; forcing auth by binding `0.0.0.0` instead exposes raw port ∴ neither bind is safe|§V40: `[mcp.remote] behind_proxy=true` decouples auth enforcement from bind addr → forces §V9 HTTPS+OIDC gate + bearer enforcement even on `127.0.0.1` bind when behind proxy; ⊥ equate loopback w/ authless-trusted; cites V9,V10,V40
B32|2026-07-19|V36/B17 (T64 M7 security suite, confirmed end-to-end): levels-tree confinement (`_is_clean_level_path`) was wired ONLY into the network discovery path (`arknights_assets._discover_level_paths`), ⊥ the local import read path — `import_stages` (`stages.py:167`) only called `normalize_level_id` then `adapter.exists`/`read_json`. A crafted `levelId`=`gamedata/levels/../../gamedata/excel/evil_level` stays within the snapshot root (∴ `LocalSnapshotAdapter._safe_path` passes: root-escape ≠ triggered) but folds back INTO `gamedata/excel/` → stage imports `levels_imported=1` reading an excel table as level data (`stage_tiles.tile_key='EVIL_EXFIL'`). `_safe_path` (root boundary) ⊥ sufficient for the nested-excel fold-back; needs the clean-path gate too|extract `_is_clean_level_path`→shared `normalization.is_clean_level_path` (single §V37 home); `import_stages` routes the normalized `level_path` through it before `adapter.exists`/`read_json`, dropping a non-clean ref (stage imported w/ no level); network discovery reuses the same fn (test_local_import_confines_levelid_to_levels_tree) ∴ V36,V37,V2
B33|2026-07-19|B5 fix incomplete (T64 M7 security suite): `ArknightsAssetsAdapter._download` caught deep-JSON `RecursionError`→`SourceAdapterError` + ran `_json_within_limits`, but the local import path `LocalSnapshotAdapter.read_json` (`local_snapshot.py:55`) caught only `json`/`UnicodeDecodeError` + applied NO depth/node caps → a hostile local snapshot (`"["*100000+"]"*100000`) fed to `arknights-mcp import` dies with an uncaught `RecursionError` traceback|extract `_json_within_limits`→shared `sources.base.json_within_limits` (single §V37 home); `read_json` catches `RecursionError`→`SourceAdapterError` + runs the shared caps, matching the network stager (test_local_adapter_rejects_deeply_nested_json) ∴ B5,V22,V37
B34|2026-07-19|V1/V30 (first real end-to-end `sync` vs live upstream @`413a81a3ff3e`): T68 hand-stages only 4 core files + 4-4's level then drives `import` ∴ full network `sync` discovery path (`arknights_assets.stage`) was NEVER exercised e2e. real `stage_table` keeps `levelId` refs for retired events (`act10d5`,`act7mini`,`act2bossrush`,`act4d0`…) whose level files are PRUNED from the snapshot → `stage()` downloads every discovered level, first pruned file 404s, `HttpsFetcher.fetch` lets raw `urllib.error.HTTPError` escape → whole `sync --server en` dies w/ uncaught traceback (import read path already tolerates a missing level via `adapter.exists` skip+warn since B32, but the sync stager aborted). same one-path-not-both class as B32/B33|typed `SourceNotFoundError(SourceAdapterError)` in `sources.base`; `HttpsFetcher.fetch` maps HTTP 404/410→`SourceNotFoundError`, other `HTTPError`/`URLError`→`SourceAdapterError` (⊥ raw traceback); `stage()` skips+counts `SourceNotFoundError` for DISCOVERED LEVEL files only (core files stay strict ∴ bad `base_url` still fails closed) + one warning w/ missing count; §V30 0-levels gate unchanged ∴ empty build still fails (test_stage_skips_pruned_level_files, test_stage_core_file_404_still_fatal) ∴ V1,V30,V37
B35|2026-07-20|V29 (next `sync --server en` after B34, verified vs live upstream @`413a81a3ff3e` across 12 level files): `_normalize_action` (`normalization.py`) treated EVERY wave action carrying a `key` as an enemy spawn (`enemy_id = ref["id"] if ref else key`), but real waves interleave `SPAWN` w/ non-spawn actions that ALSO carry a `key`: `STORY`'s `key` is a story-asset path (`activities/a001/tutorial_a001_01_a`) ∉ `enemyDbRefs` → fell through to `enemy_id = key` → leaked as a spawn → `insert_level` (`levels.py:285`) `raise ImporterError("spawn references unknown enemy 'activities/a001/…' in stage_pk=1")` → §V3/V4 fail-closed ∴ build ⊥ promoted (active DB safe) but `sync` never completes. SECOND latent bug same root: `DISPLAY_ENEMY_INFO`/`PREVIEW_CURSOR` `key`s DO name a real enemy (codex/preview cue, ⊥ spawn) → silently fabricated a phantom `count=1` spawn each (⊥ crash, corrupts `stage_enemies`). counted in 12 real levels: SPAWN=734 (all keys ∈ refs), DISPLAY_ENEMY_INFO=40, STORY=10, PREVIEW_CURSOR=2, BATTLE_EVENTS=1 ∴ SPAWN is the SOLE enemy-spawning `actionType`|`_normalize_action` gates on `action.get("actionType") == "SPAWN"` FIRST (⊥ mere presence of `key`) → drops STORY (no crash) + DISPLAY_ENEMY_INFO/PREVIEW_CURSOR/BATTLE_EVENTS (no phantom spawns); the fixture `REAL_LEVEL` now carries all 3 real non-spawn types (was only an empty-key STORY the old code also dropped ∴ never caught it) (test_wave_action_key_resolves_to_enemy_and_variant) ∴ V29
B36|2026-07-20|PRD §6.1/§11 scope + T42/T43/T44/T45 (∀ `x`), first real e2e `sync` after B35: network `sync` `CORE_FILES` (`arknights_assets.py`) fetched ONLY the 4 combat files (enemy_handbook, enemy_database, zone_table, stage_table) + discovered levels — NEVER the operator/module excel tables `import_operators` reads (character_table, skill_table) + `import_modules` reads (uniequip_table, battle_equip_table). importers treat a missing table as optional-zero (`operators.py:594`, `modules.py:386`) ∴ real `sync --server en` silently built `operators=skills=modules=0`; `get_operator`/`compare_operator_modules` return empty on a synced DB. §V30 silent-empty guard is combat-scoped ∴ ⊥ caught. same one-path-not-both class as B32/B34: operator importers validated ONLY via local `import` fixtures (`tests/fixtures/operator/`) that already carried the tables; the network sync fetch set was never checked to cover them|add `SUPPLEMENTARY_FILES` (character_table, skill_table, uniequip_table, battle_equip_table; `uniequip_data`=`uniequip_table.equipDict`, ⊥ separate file) fetched every sync via shared `_download_optional` (tolerant 404/410 skip+warn, §V37, B34 precedent — combat-only snapshot legitimately lacks them ∴ ⊥ mandatory core); introspection test asserts ∀ importer default `gamedata/*.json` path ∈ `CORE_FILES`∪`SUPPLEMENTARY_FILES` (test_core_files_cover_every_importer_table, test_stage_downloads_operator_and_module_tables, test_stage_tolerates_missing_operator_tables) ∴ V41
B37|2026-07-20|V29 (next `sync --server en` after B35/B36, verified vs live upstream clone): `_enemy_ref_map`/`_normalize_action` (`normalization.py`) resolved EVERY `enemyDbRefs` entry's spawn to the ref's own `id`, IGNORING the ref's `useDb` flag. real EN refs split two ways: `useDb:true` (23798 refs) = real `enemy_database`/handbook id; `useDb:false` (452 refs / 213 ids: `enemy_1105_tyokai_b`, `_a`, `_2a`, `nsabr_s1`, `vtskny_2_shield`…) = LEVEL-INLINE enemy variant whose stats live inline under `overwrittenData` + which is NEVER in the enemy tables — its `overwrittenData.prefabKey.m_value` names the base enemy it derives from (ALL 111 distinct prefabKeys ∈ DB/handbook ∴ resolve-to-prefab = total fn, 0 orphans). ignoring `useDb` → spawn `enemy_id="enemy_1105_tyokai_b"` → `insert_level` (`levels.py:285`) `raise ImporterError("spawn references unknown enemy 'enemy_1105_tyokai_b' in stage_pk=234")` → §V3/V4 fail-closed → `sync --server en` never completes (EN has ≥1 inline-variant stage). NOT B34's absent-upstream class (data present, shape unmodeled) — same one-flag-not-read class as B35. resolution MUST be per-ref on `useDb`, ⊥ id-membership: 10 ids are `useDb:true` in some stages + `useDb:false` inline in others; 9 inline ids → DIFFERENT prefab base across levels (`fyshp_a`→`fyshp`\|`fyshp_2`) ∴ no global inline→enemy table possible|`_enemy_ref_map` reads `useDb`: `false`+`overwrittenData.prefabKey.m_value` present → spawn resolves to base prefab (FK holds) + carries inline id as `variantId` in the allowlisted `source_fragment` (`SPAWN_ACTION_ALLOWLIST`+= `variantId`) for traceability; `useDb:true` path unchanged; genuinely-unresolvable ref (inline w/o `prefabKey` \| `useDb:true` absent from DB) STILL fails closed (⊥ fabricate enemy row). LIMITATION (§V43): inline `overwrittenData` per-variant stats (e.g. Luxury Drone 10000hp vs base 5000hp; 378/452 refs count toward clear) NOT modeled — spawn attributes to base-prefab stats; full stage-scoped modeling deferred T80 (test_inline_variant_spawn_resolves_to_prefab_base, test_inline_variant_without_prefab_stays_unresolved_fail_closed)|V29,V43
B38|2026-07-20|V29 (dig after live MCP e2e verify vs clone @`413a81a3f`): `_normalize_enemy_level` (`normalization.py`) read `enemyData.attributes.<stat>.m_value` blind, IGNORED the sibling `m_defined` flag. real `enemy_database` level entries = DELTAS over level 0: a higher level (variant) redefines only changed attrs + leaves the rest `{m_defined:false, m_value:0}` (sentinel = inherit base) → importer wrote the 0 sentinel as a real stat. 195/1824 enemies multi-level; 211 upstream high-level entries / 168 stored `enemy_levels` variant>0 rows carried false-0 res/attackInterval/moveSpeed/weight. `db/repositories/stages.py:137` threat LEFT JOIN keys on `se.enemy_level_variant` ∴ a stage spawning a level>0 variant (895 spawns) fed false-0 def/res to def_res_skew/ranged_arts/block_bypass + `get_enemy` (4-4 Crownslayer `enemy_1502_crowns` var2: res=0/speed=0/weight=0/interval=0 vs true inherited 50/1.4/1/2.8). same one-flag-not-read class as B35 (`actionType`) / B37 (`useDb`)|`_defined_m_value` gates on `m_defined` (explicit `false`=unset, absent=defined ∴ shorthand fixtures + genuine `m_defined:true` 0 both kept); `_normalize_enemy_level` drops unset cells; `_apply_level_deltas` backfills each higher level's missing mapped keys from the level-0 base (`_INHERITED_LEVEL_KEYS`); genuinely-unset stat inherits base, ⊥ 0 (test_undefined_higher_level_stats_inherit_base_not_zero, test_defined_zero_stat_is_kept_not_treated_as_unset)|V29,V44
B39|2026-07-20|V46 (T80 self-review of eb068bc): `_collect_variants` (`normalization.py`) resolved inline variants from `enemyDbRefs` ONLY + emitted one row per ref w/ NO dedup, while its sibling `_enemy_ref_map` — resolving the SAME refs into spawn `variantId`s — falls back to `enemies` when `enemyDbRefs` absent + dedups per-id last-wins. two asymmetries: (1) a level declaring refs under `enemies` → ref map emits spawn `variantId` but `_collect_variants` returns [] → variant row absent → `variant_pk` NULL → def/res/motion COALESCE overrides silently dropped back to §V43, NO error; (2) a duplicate inline id (ref map collapses last-wins) → 2 variant rows same id → `UNIQUE(stage_pk, variant_id)` collision → `insert_level` `integrity_guard`→`ImporterError` → whole candidate build dies. both latent post-T80 (pre-T80 `_collect_variants` ⊥ exist ∴ ⊥ shipped-broken, but ⊥ caught by the T80 tests either — fixture only ever used `enemyDbRefs` w/ unique ids). same two-paths-must-agree class as B26 (spawn count vs page JOIN)|`_collect_variants` mirrors `_enemy_ref_map` EXACTLY: `enemyDbRefs`→`enemies` source fallback + per-id dict last-wins dedup (`list(variants.values())`); docstring + §V46 pin the mirror invariant; also drop dead `level` key from `OVERWRITTEN_DATA_ALLOWLIST` (never read — variant level = the ref's own `level`, ⊥ `overwrittenData.level`) (test_collect_variants_reads_enemies_fallback, test_collect_variants_dedups_duplicate_inline_id)|V46
B40|2026-07-20|P1-14 (MCP surface doc audit vs review report, review pre-T80): `compare_operator_modules` tool description (`mcp/tools/module_compare.py:47`) + `models/operators.py:7,21,47` call the module levels 1/2/3 "potential levels" — but these are MODULE upgrade levels (§T45); operator potential is a SEPARATE axis (the potential-dependent talent variants nested inside a module level). a client reads the tool text literally ∴ conflates module tier w/ operator potential → wrong tool use/explanation. docstring-only (behavior correct)|rename→"module levels" both surfaces; doc-terminology regression test ∴ V48
B41|2026-07-20|P0-5 (same review): `analyze_stage(depth="detailed")` description promises "full per-enemy stat/timing context" (`mcp/tools/stage.py:231`) yet `_occurrence_full` (`:247`) + `EnemyOccurrenceFacts` (`services/stages.py:70-92`) emit enemy_class/motion_type/attack_type + timing ONLY — NO hp/atk/def/res/attack_interval/move_speed/weight. detailed ⊃ standard by class+timing, ⊥ by the stat block the wording implies ∴ contract mismatch, a client can't rely on `depth` meaning. survives T80 (occurrence facts never carried stats)|add stat block to occ facts+detailed shaper \| amend desc to emitted subset; integration test ∴ V47
B42|2026-07-20|P0-1/§V24 (same review): `services/search.py` `search_entities`/`search_stages` return bare `not_found` whether the region index is empty OR the entity truly absent — no active-snapshot check. §V24 mandates not_found\|region_unavailable\|data_stale; B24 wired the region verdict into `arknights://status` ONLY, the search tools were never updated ∴ `search(server=cn)` w/ no cn snapshot → `not_found` → client concludes the entity absent on cn (same one-surface-not-both class as B24)|region gate before search → `data_stale` (non en/cn→`unsupported_server`); test cn-search no snapshot ∴ V50
B43|2026-07-20|P0-4/§V26 (same review, verified 4-4): `analyzers/rules/lane_route.py:68` emits "Stage fields {route_count} distinct enemy routes; threats approach from several lanes" reading raw stage `route_count` (4-4 = 26 raw route RECORDS, many sharing start/end/checkpoint geometry) as if a lane tally → LLM renders "26 lanes", materially misleading. raw route-record count ≠ effective lane count (same raw-field-≠-semantic-claim class as B30/§V39 preDelay)|cluster effective lanes \| relabel "raw route records"+limitation+downgraded conf; test 26-raw ⊥ "26 lanes" ∴ V49
B44|2026-07-20|P1-9 (same review): `get_stage` route `checkpoints`=`json_load(checkpoints_json)` (`services/stages.py:480`) passes the raw source shape to the wire → an empty checkpoint set serializes as `{}` while a populated one is a list `[{…}]` ∴ the field's JSON type varies row-to-row, breaking a client that indexes it as an array|normalize checkpoints→always-array in RouteFacts shaping (empty→`[]`); test ∴ V51
B46|2026-07-21|§V5/§V54 (live `sync --server en` after T102, verified vs penguin `items?server=US`): `parse_items` (`importers/penguin_drops.py:154`) set `display_name=as_str(kept.get("name"))` — penguin top-level `name` = canonical CHINESE label (`固源岩`/`赤金`/`基础作战记录`); the English string lives in `name_i18n.en`, STRIPPED by `ITEM_ALLOWLIST` (`field_policy.py:178` = `{itemId,name,rarity,itemType}`) before `parse_items` saw it ∴ 153/155 en-region items stored w/ Chinese `display_name` → `get_stage_drops(server=en)` surfaces cn text under an en envelope (region-integrity defect: §V5 held for the fact REGION but not the display STRING). importer already knew `region` but `parse_items` didn't receive it|add `name_i18n` to `ITEM_ALLOWLIST` (name-only per-locale dict, ⊥ prose §V18); `parse_items(region=)` picks `name_i18n.<locale>` via `REGION_TO_NAME_LOCALE` (en→en, cn→zh) w/ canonical-`name` fallback; bump `FIELD_POLICY_VERSION`→2 (test_v59_en_item_uses_english_locale_name, test_v59_cn_item_uses_chinese_locale_name, test_v59_missing_locale_falls_back_to_canonical_name)|V59
B45|2026-07-20|V6/§V55 (T87-T92 diff review): `_drop_observation` (`analyzers/farming.py:124`) confidence ladder was `if expired / elif sample<floor` — MUTUALLY EXCLUSIVE ∴ a drop BOTH expired AND thin-sampled recorded ONLY the expiry limitation, silently dropping the "rate is noisy" caveat §V55 mandates. §V6 wants COMPLETE limitations, ⊥ dominant-cause-only. also `_RECOMMENDATION_THRESHOLD=0.5` (§V8 boundary) was DEAD — never referenced ∴ a future tune of any `_CONF_*` across 0.5 would silently invert §V8 (thin/expired figure reads as recommendation) w/ no guard|each conservatism condition INDEPENDENTLY lowers confidence (`min()`, lowest wins) + appends its OWN limitation ∴ expired+thin carries BOTH; module-load `assert _CONF_THIN_SAMPLE<_RECOMMENDATION_THRESHOLD<=_CONF_STABLE` + `_CONF_EXPIRED<threshold` makes §V8 executable — a bad tune fails loudly at import (test_expired_and_thin_sample_carries_both_limitations)|V6,V8,V55
B47|2026-07-21|V61/§V30 (M9 announcement review, `3d6c628..5defdca`): `importers/announcements.py` date+degradation logic had 3 defects. (a) §V61 degradation guard tripped only when `date`=`url`=`category` ALL-3-null ∀ rows, but the §T96 since/until filter keys on `date` ALONE ∴ a feed revision nulling just `date` (url/category still map) ⊥ trip guard → degraded build promotes healthy, every windowed query silently empties — the exact trap §V61 exists to close. (b) `_normalize_date` validated `1<=day<=31` for ANY month → stored impossible `2026-02-31`; §V26 ⊥ fabricate. (c) year-rollover `year = fetched.year-1 if month>fetched.month else fetched.year` assumed every entry PAST → pre-announced maintenance (Aug entry fetched Jul) stamped a year early; UTC-vs-server New-Year skew (Jan-1 entry fetched Dec-31 23:30 UTC) flipped year too|(a) guard keys on `date` alone (strictly stronger — all-3-null set already has date all-null); (b) construct via `datetime.date(y,m,d)` in try/except → `None` on ValueError (leap-yr free); (c) infer year by NEAREST of prior/current/next to `fetched_at` ∴ near-future = current-yr pre-announce, just-past-boundary rolls back. SPEC §V61 wording made per-field + nearest-year executable|V61,V30,V26
B48|2026-07-21|V33 (same M9 review): `import_announcements._insert_snapshot` INSERT into `source_snapshots` was UNGUARDED while the per-row `announcements` INSERT right below it wraps `integrity_guard`→typed `ImporterError`. deterministic `snapshot_id`=`make_snapshot_id(region, manifest_hash)` ∴ a byte-identical feed re-import on one conn collides the snapshot PK → uncaught `sqlite3.IntegrityError` escapes the typed-error discipline (§V33), masked only by the ride-along fail-open `except`|wrap the snapshot INSERT in `integrity_guard(...→ImporterError)` matching the row insert + penguin importer|V33,V3
B49|2026-07-21|V19/§V56 (same M9 review): `GetAnnouncementsInput.since/until` validated LENGTH only (`max_length=MAX_ID_LEN`), ⊥ ISO shape → a non-date bound (`since='july'`) fed the lexicographic SQL `date >= 'july'`: every ISO date starts w/ a digit (sorts before `'j'`) ∴ predicate False ∀ rows → silent empty, no error; caller ⊥ distinguish "none in range" from "malformed bound" (unlike `server`, rejected at the protocol layer)|Pydantic `field_validator` on since/until → `datetime.fromisoformat` (accepts ISO date OR datetime, validates calendar parts); a bad bound surfaces as protocol `ValidationError` at the model gate|V19,V56
B50|2026-07-21|V57/§V50 (M10 locale review, `fc73e44..1ceb9b0`): `search_entities` `locale` filter (`db/repositories/search.py:52-59`) narrows via `EXISTS` on the `operator_aliases`/`enemy_aliases` locale col, but the primary enemy importer (`importers/enemies.py`) inserts NO self-name alias (0 alias rows) — only operators self-alias (`_operator_aliases`, T98-stamped en/zh) ∴ `SearchLocale`=`Literal["en","zh","ja","ko"]` accepted `en`/`zh` (lockstep-derived from `REGION_TO_NAME_LOCALE ∪ EXTRA_LOCALE_FOR_REGION`) yet `locale=en` silently kept operators + dropped EVERY enemy; en/zh case degenerate anyway (entity's own name = its region locale ∴ ≈ `server=`). tool desc promised "entities carrying a name/alias in that locale" ∀ en/zh/ja/ko|restrict `SearchLocale`→{`ja`,`ko`} (`models/common.py`); `test_search_locale_domain_matches_field_policy_maps` derives from `EXTRA_LOCALE_FOR_REGION.values()` only; stored en/zh alias-locale tag retained (feeds §V59, ⊥ a filter value); `locale=en`/`zh` → protocol `ValidationError` (T108)|V57,V50
B51|2026-07-21|V30 (M10 locale review): `import_locale_aliases` (`importers/extra_locale_aliases.py:205`) §V30 guard `if candidate_names and inserted==0` keyed the COMBINED operator+enemy count ∴ a jp/kr source whose enemy_handbook game_id scheme mismatched (0 enemy aliases) but whose character_table matched (>0 operator aliases) → `inserted>0` → guard ⊥ trip → every enemy alias silently dropped, masked by the operator domain's success (exact per-domain silent-empty §V30 exists to catch)|guard PER DOMAIN keyed on MATCHED game_ids (a name whose pk lookup returned ≥1 entity), ⊥ physical insert count: `op_names non-empty and op_matched==0` OR `enemy_names non-empty and enemy_matched==0` → `ImporterError` naming the missed domain; a genuinely empty domain imports 0 without error. keying on MATCHED (⊥ inserted) stays correct under T109 `INSERT OR IGNORE` idempotency — a re-run suppresses dup INSERTs but the game_ids still MATCH ∴ ⊥ false-trip (T108)|V30
B52|2026-07-21|V57/§V18 (M10 locale review, FIXED this session): `_names_from_id_keyed` (`importers/extra_locale_aliases.py:119`) kept a name via `if name is not None` — a name that sanitized to `""` (all control/whitespace stripped by `apply_allowlist`) is `""` not `None` ∴ stored as an EMPTY alias row (junk FTS token) AND counted toward `inserted` → falsely satisfies the §V30 non-empty guard, masking a real scheme mismatch|`if name:` (matches `_operator_aliases`' own `if name:` convention) ∴ an empty-sanitized name contributes no alias + no false §V30 pass; applied 2026-07-21|V57,V18,V30
B53|2026-07-22|V62/§V58 (M11 banner review, robustness): `import_banners` wired into MAIN `_import_one` build (`importers/pipeline.py:180`) fail-CLOSED (⊥ ride-along) — a dup `gachaPoolId` (UNIQUE(server,game_id)) OR repeated featured char_id in one `dynMeta.attainRare6CharList` (UNIQUE(banner_pk,char_id)) → `integrity_guard`→`ImporterError` propagates → whole candidate discarded (§V3) ∴ OPTIONAL banner archive nukes MANDATORY enemy/stage/operator combat build. contradicts §V58 principle (optional domain ⊥ compromise §V3 core); banners was odd-one-out — penguin (§V58)/announcement/extra-locale ALL savepoint fail-open|wrap `import_banners` in `_import_one` under `SAVEPOINT banners` + fail-open catch: banner `ImporterError` → `ROLLBACK TO SAVEPOINT`+warn+skip, combat CONTINUES (T116); §V62 banner §V30-guard folded into fail-open catch, matches §V58|V62,V58
